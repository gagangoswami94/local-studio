import React from 'react';

const MenuBar = () => {
  return (
    <div className="menu-bar">
      <span style={{ fontSize: '14px', fontWeight: 600 }}>Local Studio</span>
    </div>
  );
};

export default MenuBar;
